const fs = require('fs');
const p = require('path').join(__dirname, '..', 'src', 'index.css');
const a = (s) => fs.appendFileSync(p, s);
a('\n');
a('.form-feedback-overlay { position: fixed; top: 6.5rem; right: 1.25rem; display: flex; flex-direction: column; gap: 0.4rem; max-width: 300px; z-index: 15; }\n');
a('.form-cue { display: flex; align-items: center; gap: 0.45rem; padding: 0.6rem 0.9rem; background: linear-gradient(135deg, rgba(16, 20, 38, 0.8), rgba(16, 20, 38, 0.6)); backdrop-filter: blur(20px) saturate(1.3); -webkit-backdrop-filter: blur(20px) saturate(1.3); border: 1px solid var(--glass-border); border-radius: 14px; font-size: 0.8rem; font-weight: 700; transition: all 0.3s ease; animation: cue-slide-in 0.35s cubic-bezier(0.34, 1.56, 0.64, 1); }\n');
a('@keyframes cue-slide-in { 0% { opacity: 0; transform: translateX(20px); } 100% { opacity: 1; transform: translateX(0); } }\n');
a('.form-cue.good { border-color: rgba(0, 255, 136, 0.35); color: var(--neon-green); box-shadow: 0 0 20px rgba(0, 255, 136, 0.1); }\n');
a('.form-cue.warn { border-color: rgba(255, 170, 0, 0.35); color: var(--neon-amber); box-shadow: 0 0 20px rgba(255, 170, 0, 0.1); }\n');
a('.form-cue.bad { border-color: rgba(255, 42, 109, 0.35); color: var(--neon-red); box-shadow: 0 0 20px rgba(255, 42, 109, 0.1); }\n');
a('.form-cue-dot { width: 7px; height: 7px; border-radius: 50%; background: currentColor; animation: pulse-glow 1.5s ease-in-out infinite; flex-shrink: 0; }\n');
a('.form-score { margin-left: auto; font-size: 1rem; font-weight: 900; filter: drop-shadow(0 0 6px currentColor); }\n');
a('\n');
a('.form-faults { display: flex; flex-direction: column; gap: 0.3rem; }\n');
a('.fault-chip { padding: 0.4rem 0.7rem; border-radius: 10px; font-size: 0.7rem; font-weight: 700; border: 1px solid; transition: all 0.3s ease; animation: cue-slide-in 0.3s ease; }\n');
a('.fault-chip.critical { background: rgba(255, 42, 109, 0.1); border-color: rgba(255, 42, 109, 0.35); color: var(--neon-red); }\n');
a('.fault-chip.warning { background: rgba(255, 170, 0, 0.1); border-color: rgba(255, 170, 0, 0.35); color: var(--neon-amber); }\n');
a('.form-meta { display: flex; flex-wrap: wrap; gap: 0.3rem; align-items: center; }\n');
a('.phase-pill { padding: 0.25rem 0.6rem; border-radius: 50px; font-size: 0.65rem; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; transition: all 0.3s ease; }\n');
a('.phase-pill.phase-setup { background: rgba(148, 163, 184, 0.1); color: var(--text-muted); }\n');
a('.phase-pill.phase-eccentric { background: rgba(0, 243, 255, 0.1); color: var(--neon-cyan); }\n');
a('.phase-pill.phase-bottom { background: rgba(157, 78, 221, 0.1); color: var(--neon-purple); }\n');
a('.phase-pill.phase-concentric { background: rgba(0, 255, 136, 0.1); color: var(--neon-green); }\n');
a('.meta-item { font-size: 0.65rem; font-weight: 600; color: var(--text-muted); background: rgba(255, 255, 255, 0.04); padding: 0.25rem 0.5rem; border-radius: 8px; border: 1px solid rgba(255, 255, 255, 0.06); }\n');
a('.meta-item.setup-warn { background: rgba(255, 170, 0, 0.08); color: var(--neon-amber); border-color: rgba(255, 170, 0, 0.2); }\n');
a('.form-invalid-ribbon { padding: 0.4rem 0.7rem; background: rgba(255, 42, 109, 0.12); border: 1px solid rgba(255, 42, 109, 0.35); border-radius: 10px; font-size: 0.7rem; font-weight: 800; color: var(--neon-red); text-transform: uppercase; letter-spacing: 1px; animation: ribbon-pulse 1.2s ease-in-out infinite; }\n');
a('@keyframes ribbon-pulse { 0%, 100% { border-color: rgba(255, 42, 109, 0.35); box-shadow: 0 0 8px rgba(255, 42, 109, 0.1); } 50% { border-color: rgba(255, 42, 109, 0.7); box-shadow: 0 0 18px rgba(255, 42, 109, 0.2); } }\n');
console.log('Chunk 6 done');
