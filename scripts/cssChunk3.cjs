const fs = require('fs');
const p = require('path').join(__dirname, '..', 'src', 'index.css');
const a = (s) => fs.appendFileSync(p, s);
a('\n');
a('.phase-indicator { display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem 1.1rem; background: rgba(0, 243, 255, 0.06); border: 1px solid rgba(0, 243, 255, 0.2); border-radius: 50px; font-size: 0.7rem; font-weight: 800; text-transform: uppercase; letter-spacing: 2.5px; color: var(--neon-cyan); transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1); }\n');
a('.phase-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--neon-cyan); animation: pulse-glow 1.5s ease-in-out infinite; flex-shrink: 0; }\n');
a('@keyframes pulse-glow { 0%, 100% { opacity: 1; box-shadow: 0 0 6px var(--neon-cyan), 0 0 12px var(--neon-cyan); } 50% { opacity: 0.5; box-shadow: 0 0 14px var(--neon-cyan), 0 0 28px var(--neon-cyan); } }\n');
a('\n');
a('.workout-state { font-size: 1.4rem; font-weight: 900; text-transform: uppercase; letter-spacing: 5px; color: #fff; text-shadow: 0 0 30px rgba(0, 243, 255, 0.6), 0 0 60px rgba(0, 243, 255, 0.3); transition: all 0.3s ease; animation: state-pop-in 0.35s cubic-bezier(0.34, 1.56, 0.64, 1); }\n');
a('@keyframes state-pop-in { 0% { opacity: 0; transform: scale(0.8) translateY(10px); filter: blur(4px); } 100% { opacity: 1; transform: scale(1) translateY(0); filter: blur(0); } }\n');
a('.workout-state.ready { color: var(--neon-amber); text-shadow: 0 0 30px rgba(255, 170, 0, 0.6); }\n');
a('.workout-state.down { color: var(--neon-cyan); }\n');
a('.workout-state.up { color: var(--neon-green); text-shadow: 0 0 30px rgba(0, 255, 136, 0.6); }\n');
a('.workout-state.complete { color: var(--neon-gold); text-shadow: 0 0 30px rgba(255, 215, 0, 0.6); }\n');
a('\n');
a('.hud-bottom { display: flex; flex-direction: column; gap: 0.75rem; align-items: center; }\n');
a('\n');
a('.exercise-selector { display: flex; gap: 0.35rem; background: linear-gradient(180deg, rgba(16, 20, 38, 0.8), rgba(16, 20, 38, 0.6)); backdrop-filter: blur(20px) saturate(1.3); -webkit-backdrop-filter: blur(20px) saturate(1.3); border: 1px solid var(--glass-border); border-radius: 50px; padding: 0.35rem; box-shadow: var(--glass-shadow), inset 0 1px 0 rgba(255, 255, 255, 0.05); }\n');
console.log('Chunk 3 done');
