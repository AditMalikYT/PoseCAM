const fs = require('fs');
const p = require('path').join(__dirname, '..', 'src', 'index.css');
const a = (s) => fs.appendFileSync(p, s);
a('\n');
a('.exercise-btn { padding: 0.55rem 1.1rem; border-radius: 50px; border: 1px solid transparent; background: transparent; color: var(--text-muted); font-weight: 700; font-size: 0.75rem; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); text-transform: uppercase; letter-spacing: 1px; min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center; gap: 0.35rem; white-space: nowrap; }\n');
a('.exercise-btn:hover { color: var(--text-main); background: rgba(255, 255, 255, 0.04); }\n');
a('.exercise-btn.active { background: rgba(0, 243, 255, 0.12); border-color: rgba(0, 243, 255, 0.4); color: var(--neon-cyan); box-shadow: 0 0 18px rgba(0, 243, 255, 0.25), inset 0 0 12px rgba(0, 243, 255, 0.08); }\n');
a('.exercise-btn.active.pullups { background: rgba(157, 78, 221, 0.12); border-color: rgba(157, 78, 221, 0.4); color: var(--neon-purple); box-shadow: 0 0 18px rgba(157, 78, 221, 0.25), inset 0 0 12px rgba(157, 78, 221, 0.08); }\n');
a('.exercise-btn .btn-icon { font-size: 0.9rem; }\n');
a('\n');
a('.controls { display: flex; gap: 0.75rem; align-items: center; }\n');
a('\n');
a('.btn-primary { padding: 0.75rem 1.6rem; border-radius: 50px; border: 2px solid var(--neon-cyan); background: rgba(0, 243, 255, 0.08); color: var(--neon-cyan); font-weight: 800; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 2px; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); min-width: 44px; min-height: 44px; position: relative; overflow: hidden; }\n');
a('.btn-primary:hover { background: var(--neon-cyan); color: #000; box-shadow: 0 0 30px rgba(0, 243, 255, 0.5), 0 0 60px rgba(0, 243, 255, 0.2); transform: translateY(-2px); }\n');
a('.btn-primary:active { transform: translateY(0) scale(0.97); }\n');
a('\n');
a('.btn-secondary { padding: 0.75rem 1.6rem; border-radius: 50px; border: 2px solid var(--neon-red); background: rgba(255, 42, 109, 0.08); color: var(--neon-red); font-weight: 800; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 2px; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); min-width: 44px; min-height: 44px; position: relative; overflow: hidden; }\n');
a('.btn-secondary:hover { background: var(--neon-red); color: #000; box-shadow: 0 0 30px rgba(255, 42, 109, 0.5), 0 0 60px rgba(255, 42, 109, 0.2); transform: translateY(-2px); }\n');
a('.btn-secondary:active { transform: translateY(0) scale(0.97); }\n');
console.log('Chunk 4 done');
